<div class="hero-container">
</div>

<div class="container">
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
    <h2 class="card" style="text-decoration: none">
      <%= cl_image_tag cocktail.photo, height: 150, width: 200, crop: :fill %>
      <%= link_to cocktail.name, cocktail_path(cocktail) %>
    </h2>
    <% end %>
  </div>
</div>
